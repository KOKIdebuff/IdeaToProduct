# IdeaToProduct v0.2 — Adaptive Idea Shaping Change Impact

> 文档类型：Change Impact Report / Design Handoff  
> 目标版本：v0.2 / target contract `0.2.0`  
> 基线版本：PRD v0.1 / SPEC v0.1 / implemented static contracts `0.1.0`  
> 状态：Design-only / Implementation Pending  
> 范围：本文件只记录产品、契约与未来实现影响，不表示 Schema、Skill、Fixture 或 Runtime 已实现 v0.2

---

# 1. Executive Summary

v0.2 将现有 `idea-intake` 从静态 Idea 结构化器升级为 **Adaptive Product Discovery Interview / Idea Shaping Skill**：系统先判断 Raw Idea 是否足以进入 Research；若不足，通过一次一个主问题的渐进式交互帮助用户澄清、挑战和收敛 Product Hypothesis，最终仍输出统一的 `idea_definition`。

本次升级不增加 Brainstorming Workflow，不复制第二套 Product Discovery 流程，不新增 Human Gate，也不改变后续 Research、Evidence、Opportunity、Product Definition、Feasibility、MVP、PRD 和 Build Readiness 的产品语义。

目标数据链为：

```text
Raw Idea
→ Adaptive Idea Shaping
→ Idea Definition（pre-research hypothesis）
→ Research Contract
→ Research Questions
→ Evidence / Claim
→ Research Synthesis
→ Opportunity
→ Product Definition（evidence-backed decision）
```

本阶段只交付 `PRD_v0.2.md`、`SPEC_v0.2.md` 与本影响报告。当前仓库的 Workflow、Schema、Fixture、Test 与 Runtime 边界保持不变。

---

# 2. Baseline Evidence

## 2.1 已实现并验证

- Core Workflow 已在 `workflow.yaml` 中定义，`idea` 仍是 `kind: skill`、`skill: idea-intake`。
- 四种 Product Profile、18 个 JSON Schema、静态验证器和契约 Fixtures 已落地。
- `WAITING_FOR_USER` 已存在于 Node Status 与 Workflow Status。
- v0.1 静态验证基线：`[OK] checked=46 errors=0`。
- v0.1 pytest 基线：`33 passed`。

## 2.2 已设计但尚未实现

- Orchestrator、Runtime CLI/API、Scheduler、State Transition Executor。
- `skills/idea-intake/` 及其他正式 Skill 目录。
- `schemas/idea.schema.json` 和 Research / Competitor / Chart 等后续业务 Artifact Schema。
- 业务级 Clear / Partial / Vague Idea Fixtures。
- Human Gate 真实交互、Interactive Attempt Resume 和端到端 P0。

## 2.3 v0.1 与 v0.2 的直接契约缺口

1. `executor_result.status` 目前只接受 `COMPLETED | FAILED`，无法表达同一 Attempt 中途等待用户。
2. `workflow-state.schema.json` 强制要求非空 `research_contract_ref`，无法表示 Idea Shaping 尚未完成的 Run。
3. `create_run` 规范默认返回已生成的 Idea / Contract refs，未覆盖 Partial / Vague Idea 的等待状态。
4. Attempt Model 没有结构化 Interaction Checkpoint、当前问题或回复历史。
5. Standard Skill Contract 没有可选 Interaction Model。
6. Idea Definition v0.1 只覆盖静态 Problem / User / Proposed Solution / Assumptions / Unknowns，不足以表达 Clarity、Scenario、Value、Mechanism、Research Seeds 和 Shaping Outcome。
7. Research Contract 尚未规范记录 Research Question 到 Assumption / Unknown / Research Seed 的来源映射。

---

# 3. PRD Impact

## 3.1 需要修改

- 文档目标、产品定位与顶层价值链加入 Idea Shaping。
- 产品背景增加 Raw Idea 质量不足和用户认知负担问题。
- G1 / G2、顶层 Workflow、Flow A 与 FR-01 更新为 Adaptive Idea Shaping。
- FR-02 增加 Research Contract Derivation 与来源追踪。
- FR-03 正式区分 Interactive Input 和 Human Gate。
- FR-24 增加 Interactive Skill Pause / Resume / Checkpoint。
- Artifact 体系明确 Idea Definition、Attempt Checkpoint 和 Product Definition 的边界。
- NFR 增加交互恢复、轮次/超时、结果诚实性和 Recommendation Bias 约束。
- 风险、版本范围、Acceptance Scenario 和最终产品原则增加 v0.2 口径。

## 3.2 保持兼容

- Core Workflow 主拓扑不变。
- 三个标准 Human Gate 与条件式 Evidence Waiver 不变。
- Research、Evidence、Verification、Research Loop、Opportunity、Product Definition、Feasibility、Proof、MVP、PRD 和 Build Readiness 的职责边界不变。
- Product Profile 不负责改变 Idea Shaping 的最大轮次或绕过交互诚实性规则。

---

# 4. SPEC Impact

## 4.1 State / Attempt

- 普通 Skill 增加 `RUNNING → WAITING_FOR_USER → RUNNING` 交互路径。
- 同一交互循环复用同一 `attempt_id`；Retry 才创建新 Attempt。
- Attempt 增加版本化、append-only 的 Interaction Checkpoint。
- Workflow State 增加独立 `current_interaction`；不得复用 `current_gate`。
- `research_contract_ref` 在 Contract 生成前允许为 `null`。
- 用户等待时间不计入自动 Attempt / Run 超时；自动执行片段的实际耗时继续累计。

## 4.2 Skill Contract

Standard Skill Contract 增加可选 `Interaction Model`：

```yaml
interaction:
  mode: adaptive
  user_input_allowed: true
  one_question_at_a_time: true
  recommendation_enabled: true
  choice_generation_enabled: true
  allow_freeform_answer: true
  max_rounds: 8
  early_exit_when_sufficient: true
```

缺少 `interaction` 时维持 v0.1 非交互 Skill 行为。`max_rounds` 是 Skill Policy，不属于 Research Contract，也不由 Product Profile 放宽。

## 4.3 Executor / API / CLI

- `executor_result.status` 目标枚举扩展为 `COMPLETED | WAITING_FOR_USER | FAILED`。
- `WAITING_FOR_USER` 必须携带一个 Interaction Request 和一个待持久化检查点，且 `error` 为 `null`、不发布最终业务 Artifact。
- Resume Executor Request 使用同一 Attempt，并携带已接受回复和最新检查点。
- Runtime API 增加 `submit_interaction_response`。
- CLI 增加等价的 `interaction submit`。
- `create_run` 允许返回 Fast Path Artifact refs，或返回 `current_interaction` 且 `research_contract_ref: null`。

## 4.4 Artifact / Research Contract

- 继续使用唯一 `idea_definition`，不新增 `idea_brief`、`brainstorm_result`、`product_concept` 或 `idea_shaping_result`。
- Interaction Checkpoint 是 Runtime 私有状态，不是业务 Artifact，不进入 Current Artifact Manifest。
- Idea Definition 是 Research 前 Hypothesis Model；Product Definition 仍是 Research 后 Evidence-backed Decision。
- Research Questions 必须从 Critical Assumptions、Researchable Unknowns 和 Research Seeds 可追溯派生。

---

# 5. Potential Schema Changes — Phase 2 Only

| Schema | 潜在改动 | 兼容性风险 |
|---|---|---|
| `common.schema.json` | 增加 `INSUFFICIENT_PRODUCT_CONTEXT`、`SCHEMA_VERSION_UNSUPPORTED`、Interaction 公共 ID/枚举或复用定义 | 当前 `schema_version` 使用 `const: 0.1.0`，需要显式版本策略 |
| `idea.schema.json` | 新增 v0.2 Idea Definition 业务契约 | 当前文件尚不存在；需要同时建立正反 Fixtures |
| `skill.schema.json` | 增加可选 `interaction` 对象及条件约束 | 必须保证无 `interaction` 的 v0.1 Skill 仍保持非交互语义 |
| `workflow-state.schema.json` | Contract ref 可空、增加 Current Interaction 与 Node Interaction refs | 影响 Create / Resume 初始状态合法性 |
| `executor-request.schema.json` | 增加 Resume Context / Accepted Response | 需要保证首次调用与 Resume 调用条件互斥且可验证 |
| `executor-result.schema.json` | 增加 `WAITING_FOR_USER` 与条件必填字段 | 是显式 Wire Contract 变化 |
| `workflow.schema.json` | 原则上仅版本与兼容性核对 | 不增加 Node、不修改主拓扑 |
| `gate.schema.json` | 预期不改 | 任何为 Idea Shaping 增加 Gate Action 的方案都应拒绝 |
| `artifact.schema.json` | 基础 Header 预期不改 | Interaction State 不得通过放宽 Header 混入 Artifact |

Phase 2 必须先决定并验证 Schema 版本注册方式。禁止只把公共 `schema_version` 常量原地改为 `0.2.0`，导致所有 v0.1 Fixture 在没有迁移说明时整体失效。

---

# 6. Potential Runtime Changes — Phase 2 Only

1. 创建 Run 时持久化 Raw Idea，并允许 Idea Attempt 在生成 Contract 前挂起。
2. 原子写入 Interaction Request、Checkpoint、Node State、Workflow State 和 Event Log。
3. 接收用户回复时验证 Run、Node、Attempt、Question 和 `state_version`。
4. 相同 Idempotency Key + 相同回复返回已有结果；冲突回复拒绝写入。
5. Resume 同一 Attempt，不增加 `attempt_count`；显式 Retry 才创建新 Attempt。
6. 用户等待期间暂停自动超时计时，恢复后继续累计自动执行片段。
7. 达到 `max_rounds` 后只允许 Early Exit、Partial Researchable 或 Insufficient Context 三种收束，不继续发出第 9 个问题。
8. `INSUFFICIENT_PRODUCT_CONTEXT` 产生部分 Idea Definition、阻塞 Idea Node 并暂停 Workflow，不触发 Research Contract。
9. Event Log 增加 Interaction Request / Response / Resume / Early Exit / Max Rounds 事件。
10. Current Manifest 只指向业务 Artifact，不登记 Attempt Checkpoint。

当前仓库尚无 Runtime 实现，因此以上全部属于目标设计和未来实施影响，不属于本阶段完成项。

---

# 7. Fixture and Test Impact — Phase 2 Only

## 7.1 Positive Fixtures

- Clear：0 次为默认，只有阻止 Research Contract 的关键歧义时允许 1 次澄清。
- Partial：预期 2–5 轮形成可研究 Idea Definition。
- Vague：能够提供 2–4 个 framing、推荐与自由输入，并在最多 8 轮内收敛或安全停止。
- Resume：进程退出后只依赖结构化 Checkpoint 恢复。
- Partial Researchable：存在显式 Unknown，但可以派生 Research Questions。

## 7.2 Required Negative Tests

1. Clear Idea 连续被追问 8 次。
2. 用户无法确定 Target User 时系统自动编造。
3. Problem 未定义便输出完整 Feature List。
4. Unknown 被写成 Validated Fact。
5. 超过 `max_rounds` 后继续交互。
6. Interactive Skill 被实现成新 Human Gate。
7. Resume 必须依赖完整聊天上下文。
8. 同一 `question_id` 接受两个冲突回复。
9. `PARTIAL_RESEARCHABLE` 没有任何可派生 Research Question。
10. Idea Definition 提前产生 Validated Differentiation 或 Confirmed Product Direction。

---

# 8. Compatibility and Migration Risks

## C1 — Schema Version Split

v0.2 文档定义目标契约 `0.2.0`，当前实现仍是 `0.1.0`。未来实现需要版本感知的 Schema Registry、并行验证或显式迁移；不得把历史实例静默解释为 v0.2。

## C2 — In-flight Run Resume

未来若存在 v0.1 Runtime State，v0.2 Runtime 不得直接假设其具有 Interaction Checkpoint。没有验证过的迁移器时，应拒绝恢复并给出明确版本错误。

## C3 — Idea Field Mapping

v0.1 `proposed_solution` 映射到 v0.2 `solution.direction_hypothesis`。该映射只改变结构名称，不得把 Hypothesis 升级成已确认方向。

## C4 — Executor Status Consumers

任何只处理 `COMPLETED | FAILED` 的 Host Adapter 都必须升级后才能声明支持 Interactive Skill。未知 `WAITING_FOR_USER` 状态必须 Fail Closed，不能误判为完成。

## C5 — Research Contract Timing

Contract ref 在 Idea Shaping 期间为空是合法状态。所有依赖 Contract 的节点必须继续由拓扑依赖阻止，不得因 nullable 字段而提前调度。

## C6 — Recommendation Bias

候选与 Recommendation 只用于降低用户表达成本。它们必须保留修改和自由输入，不得写成 Evidence、Validated Claim 或正式 Direction Decision。

---

# 9. v0.1 → v0.2 Change Summary

| 维度 | v0.1 | v0.2 目标 |
|---|---|---|
| Idea Intake | 静态结构化 | 先评估 Clarity，再自适应澄清、挑战和收敛 |
| Idea Path | 单一路径 | `CLEAR | PARTIAL | VAGUE` |
| Interaction | 未定义 | 一次一个主问题、候选 + 推荐 + 自由输入 |
| Attempt | 单次执行结果 | 同一 Attempt 支持多轮 Waiting / Resume |
| Working State | 未定义 | Attempt 私有、版本化 Checkpoint |
| Completion | 静态输出 | Early Exit、`PARTIAL_RESEARCHABLE`、安全阻塞 |
| Idea Definition | 基础 Problem/User/Solution | 增加 Scenario/JTBD/Value/Mechanism/Clarity/Research Seeds |
| Research Contract | 直接生成 Questions | 从 Assumption / Unknown / Seed 可追溯派生 |
| Human Gate | 3 个标准 + 1 个条件式 | 数量和治理职责保持不变 |
| Workflow | Idea → Contract → Research | 拓扑不变，Idea Skill 内部增加 Adaptive Interaction |
| Product Definition | Research 后决策 | 保持不变，进一步强调与 Idea Definition 的边界 |
| Wire Version | `0.1.0` | 新目标为 `0.2.0`，实施前仍保持 `0.1.0` |

---

# 10. Implementation Impact Plan — Awaiting Approval

下列顺序仅是下一阶段建议。本次文档交付完成后停止，不自动执行。

## Phase 2.1 — Version and Schema Design

- 确定 v0.1 / v0.2 Schema Registry 与迁移策略。
- 新增 `idea.schema.json`。
- 扩展 Skill、Workflow State、Executor Request / Result 和 Common Error 契约。
- 保持 Gate、Artifact Header 和 Workflow 拓扑不变。

**Done when：** 0.1 回归实例与 0.2 新实例均有明确、可重复的验证结果，不存在悬空 `$ref` 或静默版本转换。

## Phase 2.2 — Idea Intake Skill

- 创建 `skills/idea-intake/SKILL.md` 与 `skill.yaml`。
- 实现 Clarity Evaluation、Question Selection、Assumption Challenge、Completion 和 Early Exit 契约。

**Done when：** Skill Contract 能被静态校验，Clear / Partial / Vague 行为没有依赖 Runtime 私有猜测。

## Phase 2.3 — Fixtures and Contract Tests

- 增加 Clear / Partial / Vague / Resume / Partial Researchable 正例。
- 增加本报告列出的 10 个负例。
- 增加 Research Question Origin Traceability 测试。

**Done when：** 正例全部通过，负例在预期规则上确定性失败。

## Phase 2.4 — Runtime Interaction Loop

- 实现 Checkpoint、Submit、Resume、Idempotency、Timeout Accounting 和 Max Rounds 收束。
- 更新 API / CLI Adapter。

**Done when：** 同一 Attempt 可跨进程恢复；用户等待不消耗自动超时；不存在第 9 个问题或重复回复写入。

## Phase 2.5 — End-to-End Validation

- 运行三类 Idea 到 Research Contract 的端到端测试。
- 验证没有新增 Human Gate、没有顶层回边、没有提前进入 Product Definition。

**Done when：** v0.2 文档、Schema、Skill、Fixture、Runtime 和测试口径一致，并经独立一致性审查通过。

---

# 11. Phase 1 Stop Condition

当 `V0.2_CHANGE_IMPACT.md`、`PRD_v0.2.md`、`SPEC_v0.2.md` 完成一致性审查，并确认 v0.1 文件、Workflow、Schema、Fixture、Test 和 Runtime 未被修改后，本阶段结束。任何 Implementation Impact Plan 中的动作都需要新的明确批准。
